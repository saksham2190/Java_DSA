import java.util.*;

class Strings{
    public static void main (String[] args){
        Scanner sc = new Scanner(System.in);
        String gali = sc.nextLine();        // if we want single word only we use next() , if we want many words with spaces we use nextLine()
        System.out.println("Congo you are " + gali);
    }
}